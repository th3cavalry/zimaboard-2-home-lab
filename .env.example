# ZimaBoard 2 Ultimate Homelab - Environment Variables
# Copy this file to .env and customize the values below

################################################################################
# System Configuration
################################################################################

# Your ZimaBoard's IP address (auto-detected during installation)
SYSTEM_IP=192.168.1.100

# Timezone (change to your local timezone)
TZ=UTC

################################################################################
# Nextcloud Configuration
################################################################################

# Nextcloud admin password (change this!)
NEXTCLOUD_ADMIN_PASSWORD=SecurePassword123!

# Nextcloud admin username
NEXTCLOUD_ADMIN_USER=admin

# Nextcloud trusted domains (comma-separated)
NEXTCLOUD_TRUSTED_DOMAINS=localhost,192.168.1.100

################################################################################
# AdGuard Home Configuration
################################################################################

# AdGuard Home web interface port
ADGUARD_PORT=3000

# AdGuard Home DNS port
ADGUARD_DNS_PORT=53

################################################################################
# Nginx Configuration
################################################################################

# Web interface port
NGINX_PORT=80

# HTTPS port (if using SSL)
NGINX_SSL_PORT=443

################################################################################
# Storage Configuration
################################################################################

# SSD mount point for data
SSD_MOUNT_POINT=/mnt/ssd-data

# HDD mount point for cache
HDD_MOUNT_POINT=/mnt/hdd-cache

# Maximum cache size (in GB)
MAX_CACHE_SIZE=100

################################################################################
# Network Configuration
################################################################################

# Docker network subnet (for Docker installation)
DOCKER_SUBNET=172.20.0.0/16

# AdGuard Home container IP (for Docker installation)
ADGUARD_IP=172.20.0.2

# Nginx container IP (for Docker installation)
NGINX_IP=172.20.0.3

# Nextcloud container IP (for Docker installation)
NEXTCLOUD_IP=172.20.0.4

################################################################################
# Security Configuration
################################################################################

# Fail2ban jail settings
FAIL2BAN_BANTIME=3600
FAIL2BAN_MAXRETRY=5

# UFW firewall rules (additional ports to open)
ADDITIONAL_PORTS=

################################################################################
# Gaming Cache Configuration
################################################################################

# Enable gaming cache (true/false)
GAMING_CACHE_ENABLED=true

# Gaming cache domains (comma-separated)
GAMING_CACHE_DOMAINS=steam,epicgames,origin,xbox,playstation,battle.net

################################################################################
# Monitoring Configuration
################################################################################

# Enable system monitoring
MONITORING_ENABLED=true

# Monitoring retention period (in days)
MONITORING_RETENTION=30

################################################################################
# Backup Configuration
################################################################################

# Backup retention period (in days)
BACKUP_RETENTION=30

# Backup location
BACKUP_LOCATION=/mnt/ssd-data/backups

# Enable automatic backups
AUTO_BACKUP_ENABLED=true

# Backup schedule (cron format)
BACKUP_SCHEDULE="0 2 * * 0"

################################################################################
# DNS Configuration
################################################################################

# Upstream DNS servers (comma-separated)
UPSTREAM_DNS=1.1.1.1,1.0.0.1,8.8.8.8,8.8.4.4

# DNS blocking lists (comma-separated URLs)
DNS_BLOCKLISTS=https://cdn.jsdelivr.net/gh/hagezi/dns-blocklists@latest/adblock/pro.txt,https://cdn.jsdelivr.net/gh/hagezi/dns-blocklists@latest/adblock/tif.txt

################################################################################
# Performance Tuning
################################################################################

# Nginx worker processes (auto = number of CPU cores)
NGINX_WORKERS=auto

# Nginx worker connections
NGINX_CONNECTIONS=1024

# PHP-FPM max children
PHP_MAX_CHILDREN=10

# PHP-FPM memory limit
PHP_MEMORY_LIMIT=512M

################################################################################
# Optional Services
################################################################################

# Enable Watchtower for automatic updates (Docker only)
WATCHTOWER_ENABLED=true

# Enable Homepage dashboard (Docker only)
HOMEPAGE_ENABLED=true

# Enable Portainer for Docker management
PORTAINER_ENABLED=false

################################################################################
# Advanced Configuration
################################################################################

# Custom DNS records (JSON format)
CUSTOM_DNS_RECORDS={}

# Custom firewall rules
CUSTOM_FIREWALL_RULES=

# Additional Nginx configuration
CUSTOM_NGINX_CONFIG=

# Custom AdGuard Home configuration
CUSTOM_ADGUARD_CONFIG=

################################################################################
# Instructions:
# 
# 1. Copy this file: cp .env.example .env
# 2. Edit the values: nano .env
# 3. Keep your .env file private (don't commit to git)
# 4. Run the installer: sudo ./install.sh
################################################################################