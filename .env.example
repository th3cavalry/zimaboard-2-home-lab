################################################################################
# ZimaBoard Homelab Services - Environment Configuration
# Copy this file to .env and customize the values below
################################################################################

################################################################################
# System Configuration
################################################################################

# Your ZimaBoard's static IP address (e.g., 192.168.8.2)
SERVER_IP=192.168.8.2

# Timezone (change to your local timezone, e.g., America/New_York, Europe/London)
# See: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TIMEZONE=UTC

################################################################################
# Storage Configuration
################################################################################

# Path where your 2TB SSD is mounted (will be used for file share)
DATA_PATH_SSD=/mnt/ssd

# Path where your 500GB HDD is mounted (will be used for Lancache)
DATA_PATH_HDD=/mnt/hdd

################################################################################
# Samba Configuration
################################################################################

# Samba share name (visible on network)
SAMBA_SHARE_NAME=Shared

# Samba workgroup name
SAMBA_WORKGROUP=WORKGROUP

# Optional: Username for authenticated access (leave empty for guest access)
SAMBA_USER=

# Optional: Password for authenticated access (leave empty for guest access)
SAMBA_PASSWORD=

################################################################################
# AdGuard Home Configuration
################################################################################

# AdGuard Home web interface port
ADGUARD_WEB_PORT=3000

# AdGuard Home DNS port (standard DNS port)
ADGUARD_DNS_PORT=53

# AdGuard Home static IP within Docker network
ADGUARD_DOCKER_IP=172.20.0.2

################################################################################
# Lancache Configuration
################################################################################

# Lancache HTTP port
LANCACHE_HTTP_PORT=80

# Lancache HTTPS port
LANCACHE_HTTPS_PORT=443

# Lancache DNS port (for DNS intercept mode)
LANCACHE_DNS_PORT=53

# Maximum cache size in GB (adjust based on your HDD space)
LANCACHE_MAX_SIZE=400

# Lancache static IP within Docker network
LANCACHE_DOCKER_IP=172.20.0.3

################################################################################
# Samba Configuration
################################################################################

# Samba SMB ports
SAMBA_SMB_PORT=445
SAMBA_NETBIOS_PORT=139

# Samba static IP within Docker network
SAMBA_DOCKER_IP=172.20.0.4

################################################################################
# Optional Services (Uptime Kuma)
################################################################################

# Uptime Kuma web interface port
UPTIME_KUMA_PORT=3001

# Uptime Kuma static IP within Docker network
UPTIME_KUMA_DOCKER_IP=172.20.0.10

################################################################################
# Optional Services (CrowdSec)
################################################################################

# CrowdSec API port
CROWDSEC_API_PORT=8080

# CrowdSec static IP within Docker network
CROWDSEC_DOCKER_IP=172.20.0.11

################################################################################
# Network Configuration
################################################################################

# Docker network subnet
DOCKER_SUBNET=172.20.0.0/16

# Docker network gateway
DOCKER_GATEWAY=172.20.0.1

################################################################################
# DNS Upstream Servers
################################################################################

# Primary upstream DNS (Cloudflare)
UPSTREAM_DNS_PRIMARY=1.1.1.1

# Secondary upstream DNS (Cloudflare)
UPSTREAM_DNS_SECONDARY=1.0.0.1

################################################################################
# Instructions:
# 
# 1. Copy this file:        cp .env.example .env
# 2. Edit the values:       nano .env
# 3. Update SERVER_IP:      Set to your ZimaBoard's IP (e.g., 192.168.8.2)
# 4. Update storage paths:  Set DATA_PATH_SSD and DATA_PATH_HDD to your mount points
# 5. Save and deploy:       docker-compose up -d
#
# Note: Keep your .env file private (it's in .gitignore)
################################################################################